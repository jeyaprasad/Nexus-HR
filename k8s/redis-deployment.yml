apiVersion: apps/v1
kind: Deployment
metadata:
  name: nexushr-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nexushr-redis
  template:
    metadata:
      labels:
        app: nexushr-redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: redis
spec:
  ports:
    - port: 6379
  selector:
    app: nexushr-redis
